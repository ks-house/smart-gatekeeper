name: gatekeeper_app
description: "Smart Gatekeeper Tenant Smart Key Application (Flutter Hybrid Thin Client + WebView)"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_blue_plus: ^1.32.11
  webview_flutter: ^4.7.0
  permission_handler: ^11.3.1
  http: ^1.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
