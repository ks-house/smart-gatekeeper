<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kshouse.gatekeeper_app">

    <!-- Network Permission -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Bluetooth & Location Permissions -->
    <!-- Request legacy Bluetooth permissions for Android 11 (API 30) and below -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />

    <!-- Bluetooth permissions for Android 12 (API 31) and above -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <!--
      Location permissions needed for BLE scanning.

      BLUETOOTH_SCAN 에 android:usesPermissionFlags="neverForLocation" 을
      붙이지 않는 것은 의도적이다. 이 앱은 위치 권한이 실제로 필요하다:
        · Android 는 위치 권한/위치 서비스 없이는 BLE 스캔 결과를 반환하지 않는다
          (거부되어도 오류가 아니라 "0건"으로 조용히 실패한다)
        · AltBeacon 의 region monitoring 이 위치 권한을 전제로 동작한다
      화면 OFF / 백그라운드 감지를 위해 ACCESS_BACKGROUND_LOCATION 도 필요하다.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <!-- Foreground Service & Battery Optimization Exemption Permissions -->

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!--
      autoRunOnBoot(ForegroundTaskOptions) 이 실제로 동작하기 위한 권한.
      플러그인 매니페스트 병합에 의존하지 않고 앱에서 명시한다. (issue.md P1-10)
    -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!-- Declare BLE hardware feature -->
    <uses-feature android:name="android.hardware.bluetooth_le" android:required="true" />


    <!-- Intent Queries for external URL opening & APK downloads -->
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="http" />
        </intent>
    </queries>

    <application

        android:label="경성하우스 스마트키"
        android:name=".GatekeeperApplication"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!--
          APK-signature-bound personal-production policy. This permits an
          explicit manual-local bootstrap only; Target ACL proof remains
          mandatory and a signed remote rollout snapshot takes precedence.
        -->
        <meta-data
            android:name="com.kshouse.gatekeeper_app.GATT_LOCAL_MANUAL_BOOTSTRAP"
            android:value="${sgkPersonalGattBootstrap}" />

        <!-- Flutter Foreground Task Service -->
        <service
            android:name="com.pravera.flutter_foreground_task.service.ForegroundService"
            android:foregroundServiceType="connectedDevice"
            android:exported="false" />

        <!-- Wave 0 PoC: OS-managed filtered BLE wake enters native code only. -->
        <receiver
            android:name=".blewake.BleWakeScanReceiver"
            android:enabled="true"
            android:exported="false" />
        <receiver
            android:name=".blewake.BleWakeBootReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
