services:

  flutter-builder:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: gatekeeper-flutter-builder
    volumes:
      - ./:/workspace
      - ../protocol:/repo-protocol:ro
      - gradle-cache:/root/.gradle
    tty: true
    stdin_open: true
    command: sleep infinity

volumes:
  gradle-cache:
